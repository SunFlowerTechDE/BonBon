{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Die Kasse braucht nur das eigene Fenster und die eigenen Befehle.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
